###########
# Aliases #
###########

# Reload ZSH settings
alias reload-zsh="source $HOME/.zshrc"

# Update Homebrew installation
alias update-homebrew="brew update; brew upgrade; brew doctor"

# List the files and folders as a tree
# (taken from https://github.com/aziz/dotfiles/blob/master/bash/aliases/general.aliases.bash)
if [ -x "$(which tree)" ]
then
  alias tree="tree -CAFa -I 'CVS|*.*.package|.svn|.git' --dirsfirst"
fi
